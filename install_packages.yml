---

- hosts: all
  become: true
  tasks:

  - name: install neovim and tmux 
    package:
      name: 
        - "{{ neovim_package }}" 
        - "{{ tmux_package }}" 
      state: latest
      update_cache: yes
